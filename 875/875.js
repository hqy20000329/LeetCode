//暴力解决
/**
 * @param {number[]} piles
 * @param {number} H
 * @return {number}
 */


var minEatingSpeed = function (piles, H) {
    let low = 1,
        high=Math.max(...piles);
    console.log(low,high)
    //求出最大香蕉数，在某把之中
    return low
};

minEatingSpeed([3, 6, 7, 11], 8);